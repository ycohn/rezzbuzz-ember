import { limitMessages } from '../../../helpers/limit-messages';
import { module, test } from 'qunit';

module('Unit | Helper | limit messages');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = limitMessages(42);
  assert.ok(result);
});
