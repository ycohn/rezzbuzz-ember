import { prettyDate } from '../../../helpers/pretty-date';
import { module, test } from 'qunit';

module('Unit | Helper | pretty date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = prettyDate(42);
  assert.ok(result);
});
